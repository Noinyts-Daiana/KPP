name: travel_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # 👇 АКТУАЛЬНІ, СУМІСНІ ВЕРСІЇ FIREBASE (Виправлення конфлікту)
  
  # Основні пакети: використовуємо останні стабільні версії
  firebase_core: ^2.24.2       # Сучасна версія Core
  
  # Виправлення: використовуємо актуальні версії, сумісні з Core 2.x
  firebase_auth: ^4.15.2       # Актуальна версія Auth
  cloud_firestore: ^4.13.3     # Актуальна версія Firestore
  firebase_crashlytics: ^3.4.7 # Актуальна версія Crashlytics
  
  # Інші Firebase пакети
  google_sign_in: ^6.2.1
  firebase_analytics: ^10.7.4
  
  # Інші залежності
  flutter_bloc: ^8.1.3 
  intl: ^0.18.1
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/HomeScreen.jpg
    - assets/images/HomeScreenBack.jpg
    - assets/images/google_icon.jpg

  fonts:
    - family: HoltwoodOneSC
      fonts:
        - asset: assets/fonts/HoltwoodOneSC.ttf
