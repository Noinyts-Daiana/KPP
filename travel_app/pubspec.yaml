name: travel_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Трохи розширив діапазон для сумісності

dependencies:
  flutter:
    sdk: flutter
  
  # --- ОНОВЛЕНІ FIREBASE ЗАЛЕЖНОСТІ ---
  firebase_core: ^3.6.0       # Було 2.32.0
  firebase_auth: ^5.3.1       # Було 4.16.0
  cloud_firestore: ^5.4.4     # Було 4.17.5
  firebase_storage: ^12.3.2   # Було 11.6.5
  firebase_crashlytics: ^4.1.4 # Оновив для сумісності з Core
  firebase_analytics: ^11.3.3  # Оновив для сумісності з Core
  # ------------------------------------

  google_sign_in: ^6.2.1
  flutter_bloc: ^8.1.6
  intl: ^0.19.0 # Оновив до стабільної
  provider: ^6.1.2
  image_picker: ^1.1.2 # Оновив до стабільної

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/HomeScreen.jpg
    - assets/images/HomeScreenBack.jpg
    - assets/images/google_icon.jpg

  fonts:
    - family: HoltwoodOneSC
      fonts:
        - asset: assets/fonts/HoltwoodOneSC.ttf